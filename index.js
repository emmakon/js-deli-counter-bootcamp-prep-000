var katzDeli = [];

function takeANumber(katzDeliLine, name) {
  var currentLine = [];
  for (let i = 1; i < line.length; i++) {
    currentLine.push(`Welcome ${name[i]}. You are number ${line[i]} in line.`)
  }
  return newPosition
}
